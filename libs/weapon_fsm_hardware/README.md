# weapon-fsm-hardware

Optional integration package for consuming `GunRuntimeCommand` values emitted by `weapon-fsm-core`.

This package is intended to host editor playback backends and future hardware drivers without pushing
those dependencies into the core FSM/domain package.
